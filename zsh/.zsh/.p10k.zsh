# Powerlevel10k config based on the bundled Pure preset.
source "$HOME/.zsh/plugins/powerlevel10k/config/p10k-pure.zsh"

typeset -g POWERLEVEL9K_INSTANT_PROMPT=off
typeset -g POWERLEVEL9K_DISABLE_CONFIGURATION_WIZARD=true
